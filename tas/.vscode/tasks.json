{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Reset OpenOCD",
      "detail": "Kills any leftover OpenOCD server (e.g. from a crashed/force-stopped debug session) so the next debug launch can bind its ports (3333/4444/6666) instead of failing with 'address already in use'.",
      "type": "shell",
      "command": "pkill -f 'bin/openocd' || true; sleep 1",
      "windows": {
        "command": "taskkill /F /IM openocd.exe /T & exit /b 0"
      },
      "presentation": {
        "reveal": "silent",
        "panel": "shared",
        "close": true
      },
      "problemMatcher": []
    }
  ]
}
